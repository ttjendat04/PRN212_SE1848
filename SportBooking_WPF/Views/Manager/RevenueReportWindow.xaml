<Window x:Class="SportBooking_WPF.Views.Manager.RevenueReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SportBooking_WPF.Views.Manager"
        mc:Ignorable="d"
        Title="Báo cáo doanh thu" Height="500" Width="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="19*"/>
            <ColumnDefinition Width="59*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Buttons -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10" Grid.ColumnSpan="2">
            <Button Content="Báo cáo theo ngày" Width="150" Margin="0,0,10,0" Click="BtnDaily_Click"/>
            <Button Content="Báo cáo theo tháng" Width="150" Click="BtnMonthly_Click"/>
            <Button Content="Xem biểu đồ" Width="150" Click="BtnChart_Click" RenderTransformOrigin="0.758,0.842"/>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid x:Name="dgRevenue"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True" Grid.ColumnSpan="2">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ngày/Tháng" Binding="{Binding DateOrMonth}" Width="*"/>
                <DataGridTextColumn Header="Tổng doanh thu" Binding="{Binding TotalRevenue, StringFormat=N0}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
